IPE Configuration files
=======================

Place all `*.isy` under your `ipe/styles/` folder. And then, modify `prefs.lua` (it resides in your `ipe/lua`) and make the default style sheet, you may prefer `doctor.isy`.


